resolver: lts-8.5
extra-deps:
  - Ranged-sets-0.3.0
  - hasql-pool-0.4.1
ghc-options:
  postgrest: -O2 -Werror -Wall -fwarn-identities -fno-warn-redundant-constraints
nix:
  packages: [postgresql, zlib]
